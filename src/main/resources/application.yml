spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/Lubee
    username: root
    password: Ab016018!@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true   #
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: always